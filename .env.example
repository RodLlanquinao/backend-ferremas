#############################################################
# FERREMAS Backend - Configuración de Variables de Entorno
# 
# INSTRUCCIONES:
# 1. Copia este archivo como `.env` para desarrollo local
# 2. Configura las variables en Railway para producción
# 3. No incluyas el archivo .env en el control de versiones
#############################################################

# CONFIGURACIÓN GENERAL DEL SERVIDOR
#---------------------------------
# Puerto donde se ejecutará el servidor (Railway lo configura automáticamente)
PORT=3000

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# CONFIGURACIÓN DE BASE DE DATOS
#---------------------------------
# URL de conexión a PostgreSQL
# En Railway, esta variable se configura automáticamente al adjuntar un servicio PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/basededatos
DATABASE_URL=postgresql://postgres:password@localhost:5432/ferremas_dev

# CONFIGURACIÓN SSL PARA BASE DE DATOS
#---------------------------------
# Habilitar/deshabilitar SSL para conexiones a base de datos
# En producción (Railway) debe ser true, en desarrollo puede ser false
DB_SSL_ENABLED=false

# Verificar certificados SSL (false en Railway, evita problemas con certificados autofirmados)
DB_SSL_REJECT_UNAUTHORIZED=false

# Tiempo máximo de espera para conexiones a la base de datos (en milisegundos)
DB_CONNECTION_TIMEOUT=5000

# CONFIGURACIÓN DE CORS
#---------------------------------
# Orígenes permitidos para CORS (separados por comas)
# En desarrollo: * (permite todos)
# En producción: especificar dominios exactos, ej: https://mi-app.com,https://admin.mi-app.com
CORS_ORIGIN=*

# CONFIGURACIÓN DE SEGURIDAD
#---------------------------------
# Secreto para JWT (debe ser una cadena larga y aleatoria en producción)
JWT_SECRET=ferremas_development_secret_key_change_in_production

# Nivel de logging (debug, info, warn, error)
LOG_LEVEL=debug

# CONFIGURACIÓN ESPECÍFICA DE RAILWAY
#---------------------------------
# Estas variables son proporcionadas automáticamente por Railway
# No es necesario configurarlas manualmente, se incluyen como referencia

# URL de la aplicación generada por Railway
# RAILWAY_STATIC_URL=https://tu-app.up.railway.app

# Entorno de Railway (production por defecto)
# RAILWAY_ENVIRONMENT=production

# Proyecto de Railway
# RAILWAY_PROJECT_ID=tu-project-id

# CONFIGURACIÓN DE RENDIMIENTO
#---------------------------------
# Tamaño máximo de las peticiones
MAX_REQUEST_SIZE=10mb

#############################################################
# NOTAS IMPORTANTES PARA RAILWAY:
#
# 1. En Railway, configura las variables directamente en el dashboard
#    o usando CLI: railway variables set KEY=VALUE
#
# 2. DATABASE_URL se configura automáticamente al vincular PostgreSQL
#
# 3. La configuración SSL para Railway requiere:
#    - DB_SSL_ENABLED=true
#    - DB_SSL_REJECT_UNAUTHORIZED=false
#
# 4. Para depurar problemas de conexión en Railway:
#    - Verifica que la IP de Railway no esté bloqueada
#    - Confirma que SSL esté configurado correctamente
#    - Revisa los logs del servicio para errores de conexión
#############################################################

